# Testado no Python 3.11.
fastapi>=0.115
uvicorn>=0.34
webauthn>=3.0        # a criptografia de passkey (biblioteca da Duo)
pyjwt[crypto]>=2.9   # assina o token de saida
cbor2>=5.6           # so pros testes (autenticador virtual)
httpx2               # so pros testes (cliente do FastAPI)
