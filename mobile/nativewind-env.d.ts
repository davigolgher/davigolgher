/// <reference types="nativewind/types" />

// NativeWind's global stylesheet is imported for its side effect; Metro handles
// the file, TypeScript just needs to know the module exists.
declare module "*.css";
